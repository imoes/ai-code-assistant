node_modules/
out/
*.vsix
.vscode-test/

# Lokale Claude-Code-Berechtigungen (maschinenspezifisch)
.claude/settings.local.json

# Test-Sandbox: wird von test/suite.js bei jedem Lauf geloescht und neu
# angelegt. Im Repo verursacht sie nur Rauschen in jedem Commit.
test/sandbox/

# Gesprächsverlauf des Assistenten – gehört zum Arbeitsplatz, nicht ins Repo.
# Die gelernten Best Practices (.ai-assistant/PRACTICES.md) dagegen SCHON:
# sie sind Projektwissen wie AGENTS.md.
ai-code-assistant.json
